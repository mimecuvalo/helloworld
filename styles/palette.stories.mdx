import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';

import { storybook } from './palette';
import { Typography } from '@mui/material';

<Meta title="Design System/Palette" />

<Canvas>
  <Story name="Palette">
    {Object.keys(storybook).map((colorGroup) => (
      <>
        <Typography variant="h2" style={{ textTransform: 'capitalize' }}>
          {colorGroup}
        </Typography>
        <ul style={{ display: 'flex', flexWrap: 'wrap', listStyle: 'none' }}>
          {Object.keys(storybook[colorGroup])
            .reverse()
            .map((color) => (
              <li style={{ textAlign: 'center', margin: '0 2em 2em 0' }}>
                <div
                  style={{
                    display: 'flex',
                    justifyContent: 'center',
                    alignItems: 'center',
                    backgroundColor: storybook[colorGroup][color],
                    width: '100px',
                    height: '100px',
                    borderRadius: '50%',
                    color: storybook.basics.black,
                    fontSize: '12px',
                    border: `1px solid ${storybook.grey[300]}`,
                  }}
                >
                  {storybook[colorGroup][color]}
                </div>
                <span>{color}</span>
              </li>
            ))}
        </ul>
      </>
    ))}
  </Story>
</Canvas>
